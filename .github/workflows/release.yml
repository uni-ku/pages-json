name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: false
        default: ''

jobs:
  release:
    if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) || github.event_name == 'workflow_dispatch'
    name: build and release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🔖 Fetch and identify tags
        id: get_tags
        run: |
          chmod +x .github/scripts/get-tags.sh
          # 传入当前版本号：手动触发时使用输入的版本，否则使用触发标签
          CURRENT_TAG="${{ github.event.inputs.version || github.ref_name }}"
          .github/scripts/get-tags.sh --minor "$CURRENT_TAG"

      - name: 📝 Generate changelog
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x .github/scripts/generate-changelog.sh
          .github/scripts/generate-changelog.sh "${{ steps.get_tags.outputs.current_tag }}" "${{ steps.get_tags.outputs.previous_tag }}"

      - name: 🔄 Create and publish GitHub Release
        uses: ncipollo/release-action@v1.21.0
        with:
          bodyFile: changelog.md
          tag: ${{ steps.get_tags.outputs.current_tag }}

  publish:
    name: 发布到 NPM
    needs: [release]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v6

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: 🏗 Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11
          run_install: false
          cache: true

      - name: 📦 Get pnpm cache directory path
        id: pnpm-cache-dir-path
        run: echo "dir=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: 📦 Setup pnpm cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: 📦 Install dependencies
        run: pnpm i --frozen-lockfile

      - name: 📦 Build package
        run: pnpm build

      - name: 🚀 Publish to NPM
        run: |
          # 检测版本号是否为预发布版本
          VERSION=$(node -p "require('./package.json').version")
          if [[ $VERSION == *"-"* ]]; then
            # 预发布版本，使用 beta 标签
            pnpm publish --access=public --no-git-checks --provenance --tag beta
          else
            # 正式版本，使用 latest 标签（默认）
            pnpm publish --access=public --no-git-checks --provenance
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
